# Development Dependencies
# Install: pip install -r requirements-dev.txt
# IMPORTANT: Create and activate a virtual environment first:
#   python3 -m venv venv && source venv/bin/activate

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.2.0
pytest-xdist>=3.5.0
pytest-mock>=3.12.0
hypothesis>=6.85.0             # Property-based testing — finds edge cases AI agents miss

# ── Code Quality ────────────────────────────────────────────────────────────
ruff>=0.1.0
black>=23.0.0
isort>=5.13.0
mypy>=1.5.0
radon>=6.0.0                   # Cyclomatic complexity analysis

# ── Build Reproducibility ──────────────────────────────────────────────────
pip-tools>=7.3.0               # pip-compile for deterministic lock files

# ── Security ────────────────────────────────────────────────────────────────
bandit>=1.7.0
safety>=2.3.0
pip-audit>=2.6.0

# ── Pre-commit ──────────────────────────────────────────────────────────────
pre-commit>=3.6.0

# ── Debug ───────────────────────────────────────────────────────────────────
ipdb>=0.13.0
rich>=13.0.0
