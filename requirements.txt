# Core web framework
fastapi>=0.128.0
uvicorn>=0.40.0
jinja2>=3.1.3
python-multipart>=0.0.9

# Configuration
python-dotenv>=1.2.1
PyYAML>=6.0.1

# Document extraction
pypdf>=3.17.0
python-docx>=1.1.0
openpyxl>=3.1.0

# OCR (Apple Vision.framework on macOS, PyMuPDF for PDF→image)
PyMuPDF>=1.26.0
Pillow>=12.0.0
pyobjc-framework-Vision>=12.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=12.0; sys_platform == "darwin"

# Intelligence / LLM
google-generativeai>=0.3.1
anthropic>=0.39.0
requests>=2.32.5

# Vector database + embeddings
lancedb>=0.27.1
pyarrow>=14.0.0
sentence-transformers>=5.2.2
FlagEmbedding>=1.2.0
numpy>=2.0.0

# PII detection (hybrid: Presidio NER + regex)
presidio-analyzer>=2.2.360
spacy>=3.8.11

# System monitoring
psutil>=7.2.2

# File watching (kept for optional watchdog mode)
watchdog>=3.0.0

# MCP server for Claude Desktop
fastmcp>=2.14.4

# Multimodal (optional — audio/video processing)
opencv-python-headless>=4.10.0  # Video scene detection
mlx-whisper>=0.4.0; sys_platform == "darwin"  # Audio transcription (Apple Silicon)

# HTTP client (for Ollama chat integration)
httpx>=0.28.0

# Rate limiting for REST API
slowapi>=0.1.9

# macOS menu bar app
rumps>=0.4.0; sys_platform == "darwin"
